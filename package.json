{
  "name": "stoked-cenv",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "nx run-many --target=build --all",
    "build:since": "nx affected --target=build",
    "start": "lerna run start --stream",
    "test": "lerna run test",
    "test:unit": "lerna run test:unit",
    "lerna-publish": "lerna version patch --yes --force-publish --no-git-tag-version",
    "lint": "lerna run lint",
    "graph": "lerna-dependency-graph",
    "report": "istanbul report --include coverage.json --dir f3 html",
    "coverage": "yarn coverage-merge && yarn report",
    "test:integration": "lerna run test:integration",
    "list-dep-mismatches": "syncpack list-mismatches",
    "fix-dep-mismatches": "syncpack fix-mismatches",
    "tsc-version": "tsc -v"
  },
  "husky": {
    "hooks": {
      "pre-push": "yarn lint"
    }
  },
  "dependencies": {
    "@nrwl/nx-cloud": "16.0.5",
    "@nrwl/workspace": "15.9.4",
    "@types/node": "18.11.18",
    "@types/jest": "^28.1.3",
    "esbuild": "^0.17.15",
    "jest": "^28.1.3",
    "lerna": "^6.6.2",
    "nx": "15.9.4",
    "ts-node": "^10.9.1",
    "typescript": "4.9.5",
    "rimraf": "4.4.1"
  },
  "cli": {
    "packageManager": "yarn"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist": [
      "packages/lib/params"
    ]
  },
  "license": "UNLICENSED"
}
